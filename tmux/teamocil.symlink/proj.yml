session:
  name: project
  windows:
  - name: vim
    layout: even-vertical
    panes:
      - cmd: cd ~ && clear
      - cmd: cd ~ && clear
  - name: git
    layout: even-horizontal
    panes:
      - cmd: cd ~ && clear
      - cmd: cd ~ && clear
  - name: console1
    panes:
      - cmd: cd ~ && clear
  - name: console2
    panes:
      - cmd: cd ~ && clear
